# Idris Totality

Just a repository that demos `Idris` with `nix`.

## Nix Shell

```shell
nix-shell
```

## Building

```shell
idris2 ./src/Main.idr --source-dir ./src -o a.out
```

## Running

```shell
./build/exec/a.out
```